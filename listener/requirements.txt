# Job-queue worker: python listener/worker.py
# (the compose worker service reuses the app image, so these are also listed
#  in the root requirements.txt)
psycopg2-binary
paho-mqtt>=2.1
python-dotenv
