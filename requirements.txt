# Core
fastapi
uvicorn[standard]
gunicorn
python-dotenv
websockets
httpx
requests
tzdata

# Opus audio compression (Gemini PCM → Opus for cellular bandwidth)
opuslib

# Database (app + listener/worker.py job queue)
psycopg2-binary

# LLM / AI
openai
google-genai>=1.70.0

# Developer WebSocket offline STT/TTS (optional if you only use /ws/).
# Pinned: app/developer_ws/tts.py uses the piper-tts >=1.3 API (PiperVoice.load,
# chunk.audio_int16_bytes); vosk 0.3.45 ships the manylinux2014_aarch64 wheel
# the arm64 VM build needs.
vosk==0.3.45
piper-tts>=1.3,<2

# Pipecat — frame-based voice pipeline (STT → LLM → TTS orchestration).
# Pinning the minor version; bumps can change frame names (e.g. 1.2 renamed
# StartInterruptionFrame → InterruptionFrame).
pipecat-ai>=1.2,<1.3

# MQTT — listener/worker.py publishes device wake commands to Mosquitto
paho-mqtt>=2.1
