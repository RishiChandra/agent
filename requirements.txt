# Core
fastapi
uvicorn[standard]
gunicorn
python-dotenv
websockets
httpx
requests
tzdata

# Opus audio compression (Gemini PCM → Opus for cellular bandwidth)
opuslib

# Database
psycopg2-binary

# LLM / AI
openai
google-genai>=1.70.0

# Developer WebSocket offline STT/TTS (optional if you only use /ws/)
vosk
piper-tts

# Pipecat — frame-based voice pipeline (STT → LLM → TTS orchestration).
# Pinning the minor version; bumps can change frame names (e.g. 1.2 renamed
# StartInterruptionFrame → InterruptionFrame).
# [silero] declares the Silero VAD endpointing dependency (developer_ws/vad.py).
# In 1.2.x the extra is a no-op — onnxruntime is a base dep and the
# silero_vad.onnx model ships inside the wheel — but keep it in case a future
# version gates onnxruntime behind the extra again.
pipecat-ai[silero]>=1.2,<1.3

# MQTT — listener/worker.py publishes device wake commands to Mosquitto
# (replaced Azure Service Bus + IoT Hub; see SCHEDULER.md)
paho-mqtt>=2.1
