# Core
fastapi
uvicorn[standard]
gunicorn
python-dotenv
websockets
httpx
requests
tzdata

# Opus audio compression (Gemini PCM → Opus for cellular bandwidth)
opuslib

# Database
psycopg2-binary

# LLM / AI
openai
google-genai

# Azure (device SDK has wheels; service SDK optional, see below)
azure-servicebus
azure-iot-device

# Optional: install only if you need IoT Hub service-side (C2D, direct methods).
# On macOS/Python 3.12+ it often fails to build due to uamqp requiring CMake.
# pip install azure-iot-hub
